// Shared game staking config.
// When per-game admin configuration is added later, DEFAULT_STAKE_KES becomes
// the fallback used when a game has no configured default.
export const DEFAULT_STAKE_KES = 50

// Quick-select stake shortcuts shown under the stake box (KES). Tapping one
// SETs the stake to that exact amount.
export const STAKE_SHORTCUTS = [50, 100, 200, 500, 1000]
